You are working on my existing Mini Media Processing Service codebase.

IMPORTANT:
- This is an existing backend project that I have built incrementally to learn backend engineering and distributed systems.
- DO NOT rewrite the backend architecture from scratch.
- DO NOT replace technologies unless absolutely necessary.
- Inspect the existing codebase first and understand how the current system works.
- Preserve the existing architecture, conventions, APIs, MongoDB models, Redis/BullMQ queues, MinIO object storage, FFmpeg processing pipeline, HLS streaming, workers, and progress tracking.
- Make the minimum necessary backend changes to support the requirements below.
- Where you need to make changes, explain exactly what changed and why.
- I care more about understanding the architecture than hiding complexity behind abstractions.

==================================================
PART 1 — BUILD A SIMPLE REACT FRONTEND
==================================================

Create a React frontend for the existing backend.

Use a minimalist visual style inspired by the general usability and information hierarchy of YouTube, but DO NOT copy YouTube's branding, exact UI, logos, or visual assets.

The UI should be clean, simple, and functional.

Prioritize functionality over visual complexity.

The frontend should have:

1. Authentication pages
   - Sign up
   - Login
   - Logout
   - Basic session/auth persistence

2. Main dashboard
   - Display all public videos
   - Videos should be displayed in chronological order of upload
   - DO NOT build a personalized recommendation/FYP system
   - Every authenticated user should see the same public video feed
   - Each video card should display:
       - Thumbnail
       - Video title
       - Uploader/user name
       - Upload date/time if available
       - Relevant basic metadata if useful

3. Video viewing page
   - Play the processed video using the backend's existing HLS functionality
   - Use an HLS-compatible player/library where necessary
   - Do not bypass the existing media pipeline
   - The frontend should consume the existing HLS playlist generated by the backend

4. Upload page / upload UI
   - Authenticated users should be able to upload a video
   - Show upload progress if the existing API exposes it
   - After upload, show the processing state
   - Show processing progress using the existing progress-tracking API
   - Once processing completes, allow the user to view the video

5. Basic navigation
   - Dashboard
   - Upload
   - Login/signup state
   - User identity
   - Logout

Keep the UI deliberately simple.
Do not spend excessive effort on animations, complex responsive layouts, recommendation algorithms, comments, likes, subscriptions, search, playlists, or other YouTube features.

==================================================
PART 2 — MULTI-USER AUTHENTICATION
==================================================

Implement authentication because multiple users will be testing the service.

IMPORTANT:
I have personally implemented authentication in several separate projects already, so do not over-engineer the auth system or turn this into a separate giant project.

Build a conventional secure authentication system that integrates cleanly with the existing backend.

Requirements:

1. User registration

Allow a user to provide:

- username
- email
- password

Store users in MongoDB.

Passwords MUST NOT be stored in plaintext.

Use an appropriate password hashing algorithm/library.

2. Login

Allow users to log in using their credentials.

Use the authentication mechanism that best fits the existing backend architecture.

Prefer a simple, maintainable implementation rather than adding unnecessary complexity.

3. Authentication middleware

Authenticated requests should be able to identify the current user.

The authenticated user should be available to controllers/services in a clean way, e.g.:

req.user

or the equivalent pattern already used in the codebase.

4. Email

When a user registers, send an email to the email address they provided.

The email can be a simple account-registration / verification email.

Use an external email provider or SMTP configuration if appropriate.

DO NOT hardcode email credentials.

Put all required configuration in environment variables.

If email verification is implemented, make the flow simple and functional.

If the existing project already has a suitable email mechanism, reuse it.

5. User model

Create or modify the User model as necessary.

At minimum it should contain appropriate fields for:

- username
- email
- password hash
- createdAt
- updatedAt

Use unique indexes/constraints where appropriate.

Do not store plaintext passwords.

==================================================
PART 3 — CONNECT USERS TO VIDEOS
==================================================

Modify the existing Video model so every uploaded video has an uploader.

For example:

uploader: ObjectId → User

Use the appropriate MongoDB/Mongoose reference pattern based on the existing codebase.

Every time an authenticated user uploads a video:

1. Identify the authenticated user.
2. Store their user ID as the video's uploader.
3. Preserve the existing upload → object storage → queue → worker pipeline.

Example conceptual document:

Video:

{
    title: "...",
    uploader: ObjectId("..."),
    status: "queued",
    progress: 0,
    ...
}

User:

{
    username: "...",
    email: "...",
    passwordHash: "...",
    ...
}

==================================================
PART 4 — PUBLIC VIDEO DASHBOARD
==================================================

The dashboard should show public videos regardless of who uploaded them.

There should NOT be a per-user personalized feed.

For now:

GET /videos

(or whatever equivalent route fits the existing backend)

should return public/completed videos ordered by upload time.

Sort:

createdAt descending

unless the existing project has a better equivalent timestamp.

The API should populate or otherwise provide the uploader's username.

The frontend should display something like:

Video title
Uploader username
Upload time

Conceptually:

------------------------------------------------
| Thumbnail                                    |
|                                              |
| Video Title                                  |
| @UploaderName                                |
| Uploaded X minutes ago                       |
------------------------------------------------

Again:

ALL users see the same public video list.

Do not implement:
- recommendations
- FYP
- personalization
- subscriptions
- social graph
- likes
- comments
- ranking algorithms

==================================================
PART 5 — VIDEO VISIBILITY
==================================================

Add a simple visibility/public field to the Video model if the existing system does not already have one.

For example:

visibility: "public"

or

isPublic: true

Keep it simple.

The dashboard should only return videos that are public AND successfully processed.

Do not expose incomplete/failed private processing artifacts as public videos.

==================================================
PART 6 — PRESERVE THE EXISTING MEDIA PIPELINE
==================================================

DO NOT replace the existing media architecture.

The upload flow should remain conceptually:

Authenticated User
      ↓
React frontend
      ↓
Express API
      ↓
Multer / upload handling
      ↓
MinIO
      ↓
MongoDB metadata
      ↓
Redis / BullMQ
      ↓
Worker
      ↓
FFmpeg
      ↓
Metadata + thumbnail + resolutions + HLS
      ↓
MinIO
      ↓
MongoDB status/progress update
      ↓
Frontend
      ↓
HLS playback

If the current implementation differs, adapt the above requirements to the existing implementation instead of rewriting everything.

==================================================
PART 7 — MONGODB CHANGES
==================================================

Inspect the current MongoDB/Mongoose models and determine exactly what changes are required.

Document:

- New User collection/model
- Changes to Video
- uploader reference
- public/visibility field
- indexes
- any migration concerns
- any existing documents that will not have an uploader

If existing videos exist without an uploader, decide on a sensible migration strategy.

Do NOT silently destroy existing data.

Explain any MongoDB commands or migration scripts that I need to run.

Include:

- indexes to create
- collections that will be created
- schema changes
- seed/test data if useful

==================================================
PART 8 — MINIO CHANGES
==================================================

Inspect the current MinIO integration.

Determine whether any changes are required.

Keep the existing object-storage architecture.

If useful, organize object keys logically, for example:

videos/{videoId}/original/...
videos/{videoId}/thumbnail/...
videos/{videoId}/1080p/...
videos/{videoId}/720p/...
videos/{videoId}/480p/...
videos/{videoId}/hls/...

BUT:

Do not change the existing object-key structure unnecessarily.

If changes are required, document:

- bucket(s)
- object-key conventions
- public/private access
- presigned URL behavior
- required bucket policies
- MIME types/content types
- CORS requirements for browser playback, if applicable

Be especially careful with HLS:
- .m3u8
- .ts segments

Ensure the correct Content-Type values are used.

Explain exactly what I need to configure in MinIO.

==================================================
PART 9 — REDIS / BULLMQ CHANGES
==================================================

Inspect the current Redis/BullMQ implementation.

Do not introduce another queue technology.

Determine whether authentication/user-related changes affect the queue.

Preserve the existing queue and worker architecture.

Document:

- Redis connection requirements
- queue names
- worker configuration
- concurrency configuration
- retry behavior
- stalled job behavior
- any environment variables
- what happens when Redis restarts

If any changes are required, implement them minimally.

==================================================
PART 10 — API CONTRACT
==================================================

Inspect the current API.

Do not blindly invent duplicate endpoints.

Reuse existing endpoints wherever possible.

Produce a clear list of the final API endpoints after your changes.

At minimum I should have functionality equivalent to:

AUTH
POST /auth/register
POST /auth/login
POST /auth/logout (if applicable)
GET /auth/me

VIDEOS
POST /videos
GET /videos
GET /videos/:id
GET /videos/:id/stream (or equivalent)
GET /videos/:id/progress (if needed)

Adapt the exact paths to the existing codebase.

For every endpoint document:

- method
- path
- authentication requirement
- request body
- response body
- errors

==================================================
PART 11 — DEPLOYMENT
==================================================

This is VERY IMPORTANT.

I am using this module specifically to learn deployment and understand how a distributed backend is deployed.

Give me a comprehensive deployment guide customized to THIS CODEBASE.

DO NOT give me a generic "deploy a Node app" tutorial.

First inspect the repository and determine:

- how the API starts
- how workers start
- how MongoDB is configured
- how Redis is configured
- how MinIO is configured
- how FFmpeg is installed/invoked
- where temporary files are stored
- how HLS files are served
- how environment variables are loaded
- frontend build process
- CORS configuration
- authentication/session configuration

Then explain how to deploy each component separately.

The final deployment architecture should conceptually be:

                    Internet
                       │
                       ▼
                React Frontend
                       │
                       ▼
                  API Server
                 /     |      \
                /      |       \
               ▼       ▼        ▼
          MongoDB    Redis     MinIO/S3
                       │
                       ▼
                    Workers
                       │
                     FFmpeg
                       │
                       ▼
                  Object Storage
                       │
                       ▼
                     CDN
                       │
                       ▼
                    Browser

Deployment components:

1. React frontend
2. API server
3. MongoDB
4. Redis
5. Worker(s)
6. MinIO OR production object storage such as S3-compatible storage
7. Nginx if useful
8. Cloudflare CDN if useful

For each component explain:

- What it does
- Where it should run
- How it communicates with other components
- Required ports
- Environment variables
- Authentication/security considerations
- Persistence requirements
- Scaling strategy
- Failure modes
- Health checks
- Logs
- How to deploy it
- How to update it
- How to restart it
- What should NOT be publicly exposed

==================================================
PART 12 — DEPLOYMENT OPTIONS
==================================================

Prefer a simple deployment path suitable for a student project.

Give me:

A. Recommended deployment architecture

B. A cheaper/easier alternative if appropriate

C. What I should use for:
   - frontend hosting
   - API hosting
   - worker hosting
   - MongoDB hosting
   - Redis hosting
   - object storage
   - CDN

Do not optimize for enterprise complexity.

Optimize for:

- learning
- low cost
- simplicity
- ability to understand each component
- ability to scale workers independently

If Docker is useful, explain where it should be used.

If Dockerfiles are required, create them.

If Docker Compose is useful for local development, preserve/create it separately from production deployment.

==================================================
PART 13 — ENVIRONMENT VARIABLES
==================================================

Create/update:

.env.example

with every required variable.

Group them logically:

SERVER
DATABASE
REDIS
MINIO / STORAGE
AUTH
EMAIL
FRONTEND
CORS
etc.

Do not expose real credentials.

Also identify which variables are:

- frontend-safe/public
- server-only/private

==================================================
PART 14 — SECURITY REVIEW
==================================================

Before declaring the work complete, inspect the implementation for obvious deployment/security issues.

Check:

- plaintext passwords
- exposed secrets
- unsafe CORS
- public MongoDB
- public Redis
- public MinIO admin API
- unsafe object access
- missing authentication
- insecure JWT/session handling
- unrestricted file upload
- missing file size limits
- path traversal
- unsafe filenames
- MIME type assumptions
- command injection through FFmpeg arguments
- publicly exposed internal ports

Do not turn this into a giant security project.

Fix obvious issues and document remaining limitations.

==================================================
PART 15 — DEPLOYMENT DOCUMENTATION
==================================================

Create a comprehensive deployment guide in the repository, for example:

docs/DEPLOYMENT.md

It should be written specifically for this project.

It must include:

1. Architecture overview
2. Local development setup
3. Environment variables
4. MongoDB setup
5. Redis setup
6. MinIO/storage setup
7. API deployment
8. Worker deployment
9. FFmpeg installation
10. Frontend deployment
11. CORS configuration
12. Authentication/email configuration
13. HLS configuration
14. CDN configuration if used
15. Health checks
16. Logging
17. Scaling workers
18. Updating deployments
19. Backups/persistence
20. Troubleshooting
21. Common failure scenarios
22. Final architecture diagram

Include actual commands wherever possible.

Do not just say "deploy the API to a cloud provider."

Tell me what to click/configure/run.

==================================================
PART 16 — HEALTH CHECKS
==================================================

Add appropriate health/readiness endpoints if the backend does not already have them.

For example:

GET /health

It should tell me whether the API itself is alive.

If appropriate, have a deeper readiness check that verifies dependencies such as:

- MongoDB
- Redis
- MinIO

But don't make the basic liveness endpoint fail just because an external dependency is temporarily unavailable.

Explain the distinction between:

- liveness
- readiness

==================================================
PART 17 — TEST THE FINAL SYSTEM
==================================================

After implementing everything, test the complete flow.

Create several test users.

For example:

User A
User B
User C
User D
User E

Have them upload videos.

Verify:

1. Registration works.
2. Email is sent.
3. Login works.
4. Each upload is associated with the correct uploader.
5. Videos appear publicly for all users.
6. Videos are ordered by upload time.
7. Uploader name appears correctly.
8. Processing jobs enter Redis.
9. Workers process them.
10. MongoDB progress updates.
11. Multiple workers can process simultaneously.
12. MinIO stores original and processed assets.
13. HLS playback works.
14. Unauthorized users cannot perform protected actions.
15. One user's videos do not disappear for other users.
16. Restarting the API does not destroy state.
17. Restarting workers does not destroy queued jobs.

==================================================
IMPORTANT DEVELOPMENT RULES
==================================================

1. INSPECT FIRST.
   Before changing code, understand the current implementation.

2. DO NOT REWRITE.
   Preserve the architecture I have already built.

3. DO NOT DUPLICATE.
   If an existing service/function already solves something, extend it.

4. KEEP RESPONSIBILITIES SEPARATE.
   Controllers should not become giant business-logic files.
   Workers should not contain HTTP logic.
   Storage logic should remain in storage services.
   MongoDB access should remain in models/services.

5. DO NOT LEAK INFRASTRUCTURE DETAILS INTO THE FRONTEND.
   The frontend should communicate with the API, not directly manipulate MongoDB/Redis.

6. DO NOT EXPOSE INTERNAL SERVICES.
   MongoDB, Redis, MinIO admin interfaces, and worker processes should not be publicly accessible.

7. KEEP THE UI SIMPLE.
   This is a backend engineering project, not a frontend portfolio project.

8. AUTHENTICATION IS NOT THE LEARNING OBJECTIVE.
   Implement it cleanly and securely, but don't spend disproportionate effort making it fancy.

9. DO NOT ADD FEATURES I DID NOT REQUEST.
   No recommendations.
   No likes.
   No comments.
   No subscriptions.
   No search engine.
   No personalized feed.
   No notifications.
   No social graph.

10. DOCUMENT EVERY ARCHITECTURAL CHANGE.

==================================================
FINAL OUTPUT
==================================================

When finished, give me:

A. Summary of what you changed

B. Files created

C. Files modified

D. MongoDB changes

E. Redis/BullMQ changes

F. MinIO/storage changes

G. Authentication flow

H. Final API endpoint list

I. Final architecture diagram

J. Environment variables

K. Local setup instructions

L. Production deployment guide

M. Scaling strategy

N. Security considerations

O. Known limitations / things I intentionally did not build

P. Exact commands I need to run to test the complete system

MOST IMPORTANT:

Do not merely tell me what I should do.

Actually implement the frontend, authentication integration, required backend changes, deployment configuration/documentation, and any necessary infrastructure configuration in the repository.